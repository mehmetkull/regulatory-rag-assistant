# Kurumsal Mevzuat Asistanı — Python bağımlılıkları
#
# Sürümler, sistemin fiilen doğrulandığı kümeye sabitlenmiştir.
# Doğrulanan ortam: Python 3.14 / Windows x64 / yalnızca işlemci.
#
# KAPALI AĞA KURULUM
#   Hedef sunucunun paket deposuna erişimi yoktur; wheel dosyaları önceden
#   internete bağlı bir makinede indirilip aktarılmalıdır:
#
#     python -m pip download -r requirements.txt -d ./wheels
#     python -m pip install --no-index --find-links=./wheels -r requirements.txt
#
#   Sürüm sabitleme burada bir üslup tercihi değildir: bu dosyadan kurulan iki
#   ortam aynı davranmalıdır, aksi hâlde geliştirme makinesinde yeniden
#   üretilen bir hatanın sunucuda geçerli olduğuna güvenilemez.
#
# torch NOTU
#   Ekran kartı olmayan sunucuda işlemci wheel'i açıkça kurulmalıdır. CUDA
#   derlemesi yaklaşık 2 GB yer kaplar ve kart yokken hiçbir şey kazandırmaz:
#     python -m pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu

# --- Web katmanı ---
fastapi==0.136.1
uvicorn==0.47.0
pydantic==2.13.4

# --- Yerel dil modeli çıkarımı (GGUF, işlemcide çalışabilir) ---
llama-cpp-python==0.3.34

# --- Erişim: vektör deposu, gömme, yeniden sıralama, parçalama ---
chromadb==1.5.9
sentence-transformers==5.6.1
transformers==5.14.1
torch==2.12.0
langchain-text-splitters==1.1.2
numpy==2.4.2

# --- Belge alımı ---
pypdf==6.14.2
python-docx==1.2.0

# --- Portal içeriğinin çekilmesi ---
httpx==0.28.1
beautifulsoup4==4.15.0

# --- Yardımcılar ---
PyYAML==6.0.3
loguru==0.7.3
psutil==7.2.2
